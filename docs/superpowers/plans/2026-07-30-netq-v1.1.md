# netq v1.1 Implementation Plan

> **For agentic workers:** Execute inline in this session. Do **not** git commit/push unless the user explicitly asks.

**Goal:** Ship netq 1.1 with TLS, DNS compare, watch, doctor scoring, UX polish, and docs refresh.

**Architecture:** Keep `src/lib/*` pure logic + `src/commands/*` shared by CLI and interactive menu. Extend args/help/main without rewriting the router.

**Tech Stack:** Node.js 20+, existing chalk / cli-table3 / @inquirer / ora; TLS via `tls.connect`.

## Global Constraints

- JSON envelope stays `{ ok, command, ts, data }`
- No backward-compat shims; no redundant try/catch fallbacks
- Standardize docstrings or omit comments
- Use existing patterns in `src/commands/*.js`
- Do not commit unless user asks

---

### Task 1: TLS lib + command

**Files:**
- Create: `src/lib/tls.js`, `src/commands/tls.js`, `tests/lib.tls.test.js`, `tests/commands.tls.test.js`
- Modify: `src/lib/index.js`, `src/commands/index.js`, `src/lib/index.d.ts`

**Produces:** `tlsCheck(host, { port?, timeoutMs? }) -> Promise<Object>`

- [ ] Implement `tlsCheck` with `tls.connect` + getPeerCertificate
- [ ] Command layer with spinner / kvTable / printJson
- [ ] Unit tests for daysRemaining and ok semantics (mock or pure helpers)

### Task 2: DNS compare

**Files:**
- Create: `src/lib/dns-compare.js`, `src/commands/dns-compare.js`, `tests/lib.dns-compare.test.js`
- Modify: lib/commands index, types

**Produces:** `dnsCompare(host, { type? }) -> Promise<{ host, type, sources, consistent, ok }>`

### Task 3: Doctor score + TLS in doctor

**Files:**
- Create: `src/lib/score.js`, `tests/lib.score.test.js`
- Modify: `src/commands/doctor.js`

### Task 4: Watch command

**Files:**
- Create: `src/lib` helpers if needed, `src/commands/watch.js`
- Modify: args (interval), main, help, menu

### Task 5: Wire CLI + recentHost + menu UX

**Files:**
- Modify: `src/cli/args.js`, `help.js`, `main.js`, `storage.js`, `storage.d.ts`, `interactive/menu.js`, `tests/cli.args.test.js`, `tests/storage.test.js`

### Task 6: Docs + README + CHANGELOG + version bump

**Files:**
- Modify: `docs/index.html`, `README.md`, `CHANGELOG.md`, `package.json`

### Task 7: Verify

- [ ] `npm test` and `npm run lint` pass
